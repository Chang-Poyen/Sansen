// Static Markdown + TeX rendering. No runtime CDN, fetch or user-data upload.
import fs from 'node:fs';
import path from 'node:path';
import {Marked} from '../tmp/ch02/web-deps/node_modules/marked/lib/marked.esm.js';
import katex from '../tmp/ch02/web-deps/node_modules/katex/dist/katex.mjs';
const out='output/derivations/ch02';
const mods=JSON.parse(fs.readFileSync(`${out}/modules.json`));
const coverage=JSON.parse(fs.readFileSync(`${out}/coverage.json`));
const diffs=JSON.parse(fs.readFileSync(`${out}/discrepancies.json`));
const verify=JSON.parse(fs.readFileSync(`${out}/verification.json`));
const simPath='output/simulations/ch02/module_summary.json';
const simGroups=fs.existsSync(simPath)?JSON.parse(fs.readFileSync(simPath)):{};
const hasSim=Object.keys(simGroups).length>0;
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const marked=new Marked({gfm:true});let mathCount=0;
function md(text){
 const math=[];
 const protect=text.replace(/\$\$([\s\S]*?)\$\$|(?<![\\$])\$(?!\$)((?:\\.|[^$\n])+?)\$/g,(all,block,inline)=>{
  const display=block!==undefined, tex=display?block:inline;
  const rendered=katex.renderToString(tex.trim(),{displayMode:display,throwOnError:true,strict:'ignore',trust:false,output:'htmlAndMathml'});
  const key=`CH02MATHTOKEN${math.length}END`;math.push({key,rendered,display});mathCount++;
  return display?`\n\n${key}\n\n`:key;
 });
 let result=marked.parse(protect);
 for(const {key,rendered,display} of math){
  if(display)result=result.replace(`<p>${key}</p>`,rendered);
  result=result.replaceAll(key,rendered);
 }
 return result;
}
fs.mkdirSync(`${out}/assets/katex`,{recursive:true});
fs.copyFileSync('tmp/ch02/web-deps/node_modules/katex/dist/katex.min.css',`${out}/assets/katex/katex.min.css`);
fs.copyFileSync('tmp/ch02/web-deps/node_modules/katex/LICENSE',`${out}/assets/katex/LICENSE`);
fs.cpSync('tmp/ch02/web-deps/node_modules/katex/dist/fonts',`${out}/assets/katex/fonts`,{recursive:true});
const css=`:root{color:#183c42;background:#f1f5f2;font:16px/1.8 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0}a{color:#17686b;text-underline-offset:3px}header{background:#153c40;color:white;padding:30px 5vw;border-bottom:5px solid #d7b86d}header a{color:#d9eeea}header .eyebrow{letter-spacing:2px;font-size:11px;color:#c0d8d2}header h1{margin:7px 0;font-size:clamp(24px,3vw,35px);font-weight:650}header p{color:#c6ded8;margin:5px 0;max-width:1000px}.layout{max-width:1570px;margin:auto;display:grid;grid-template-columns:270px minmax(0,1fr)}nav{position:sticky;top:0;max-height:100vh;overflow:auto;padding:25px 20px;background:#e7efea}nav a{display:block;padding:7px 12px;margin:3px 0;font-size:13px;line-height:1.6;text-decoration:none;border-radius:4px}nav a.active{background:white;border-left:3px solid #b08d42;color:#173f42;font-weight:650}main{min-width:0;padding:35px clamp(20px,4vw,65px);max-width:1230px}article,.card{background:#fff;border:1px solid #d5dfd9;border-radius:8px;padding:clamp(20px,3vw,40px);margin-bottom:22px}h1,h2,h3{line-height:1.4}article h1{font-size:27px}article h2{font-size:22px;margin-top:38px;padding-top:12px;border-top:1px solid #dce6e0}article h3{font-size:18px;margin-top:30px}p{overflow-wrap:anywhere}table{border-collapse:collapse;width:100%;font-size:14px;margin:22px 0;display:block;overflow-x:auto}th,td{border-bottom:1px solid #dce5df;padding:11px 13px;text-align:left;vertical-align:top}th{background:#edf3ef;font-weight:600}tbody tr:hover{background:#fafcfb}article img{display:block;width:100%;height:auto;margin:25px auto;background:white}.katex-display{overflow-x:auto;overflow-y:hidden;padding:12px 4px;max-width:100%;font-size:1.02em}.katex{font-size:1.06em}blockquote,.notice{border-left:4px solid #b9994d;background:#faf4e5;margin:20px 0;padding:14px 20px;font-size:14px}.muted{color:#60766e;font-size:13px}.tag{background:#e0efe6;color:#2d6551;padding:3px 9px;border-radius:5px;font-size:11px;white-space:nowrap}.tag.issue{background:#f6ecd4;color:#7c5b16}.stats{display:flex;gap:30px;flex-wrap:wrap;margin:22px 0}.stats strong{display:block;font-size:30px}.stats span{font-size:12px;color:#627c71}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px}.tile{display:block;background:white;border:1px solid #d5dfd9;border-radius:7px;padding:22px;text-decoration:none;color:inherit}.tile h3{font-size:18px;margin:8px 0}.tile p{font-size:14px;color:#5d766a;line-height:1.8}.tile:hover{border-color:#387c70}.source-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:14px}.source-grid img{width:100%;height:auto}.source-grid figure{margin:10px 0}.source-grid figcaption{font-size:12px;color:#5e756d}.links{display:flex;gap:16px;flex-wrap:wrap;font-size:13px;margin-bottom:18px}summary{cursor:pointer;font-weight:600}details{margin:18px 0}pre{background:#eff4f1;padding:14px;white-space:pre-wrap;overflow-wrap:anywhere}code{font-size:.9em}footer{font-size:12px;color:#698174;padding:25px 0}.next{display:flex;justify-content:space-between;gap:20px;margin:25px 0}html{scroll-behavior:smooth}@media(max-width:900px){.layout{grid-template-columns:1fr}nav{position:relative;max-height:245px;border-bottom:1px solid #ccdcd2}nav a{display:inline-block}main{padding:22px 15px}header{padding:25px 20px}.katex-display{font-size:.9em}article{padding:20px 16px}table{font-size:13px}}@media print{nav,header,.links,.next{display:none}.layout{display:block}main{max-width:none;padding:0}article{border:0}body{background:white}details{display:none}.katex-display{overflow:visible;font-size:.85em}h2{break-after:avoid}}`;
fs.writeFileSync(`${out}/assets/ch02.css`,css);
const sfgMods=mods.filter(m=>m.sfg),otherMods=mods.filter(m=>!m.sfg&&m.id!=='00-conventions');
const navLink=(m,active)=>`<a href="${m.id}.html" class="${m.id===active?'active':''}">${m.id.slice(0,2)} · ${esc(m.title)}</a>`;
const nav=active=>`<nav aria-label="第二章推导"><a href="index.html" class="${active==='index'?'active':''}">第二章总览</a><a href="00-conventions.html" class="${active==='00-conventions'?'active':''}">共同模型与假设</a><h3 style="font-size:14px;padding:12px">SFG 分析案例</h3>${sfgMods.map(m=>navLink(m,active)).join('')}<details${otherMods.some(m=>m.id===active)?' open':''}><summary>其他推导（${otherMods.length} 组）</summary>${otherMods.map(m=>navLink(m,active)).join('')}</details><a href="discrepancies.html">教材差异与待厘清</a><a href="../../extraction/index.html">案例与幻灯片数据库 ↗</a></nav>`;
function page(title,active,body){return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · Sansen 第二章</title><link rel="stylesheet" href="assets/katex/katex.min.css"><link rel="stylesheet" href="assets/ch02.css"></head><body><header><div class="eyebrow">ANALOG DESIGN ESSENTIALS · CHAPTER 02</div><h1>${esc(title)}</h1><p>从电路方程到教材结论 · 每步必要近似 · 原图与讲解可追溯</p></header><div class="layout">${nav(active)}<main>${body}<footer>本地静态文档 · 所有字体与图片保存在本地。数值曲线来自指定模型，${hasSim?'理论推导曲线与 Spectre 结果分开标示；已增加 MOS1 教学模型仿真，尚无 晶圆厂 PDK 验证。':'尚未进行 SPICE／PDK 验证。'}<br><a href="README.md">使用与重建说明</a> · <a href="verification.json">代数验算记录</a> · <a href="coverage.json">逐张覆盖索引</a></footer></main></div></body></html>`;}
const data={};
for(const [i,m] of mods.entries()){
 const markdown=fs.readFileSync(`${out}/${m.id}.md`,'utf8');let html=md(markdown);
 const sim=simGroups[m.id];
 if(sim)html+=`<section class="notice"><h2>Spectre 仿真结果</h2><p>${esc(sim.scope_zh)}</p><p>线性 AC ${sim.linear_count} · MOS 电路 ${sim.mos_count} · 极零 ${sim.pz_count} · 瞬态 ${sim.transient_count}：所列检查全部通过。</p><p>案例：${sim.case_ids.map(esc).join('、')}</p><p><a href="${sim.html}">查看本组波形、模型条件与 CSV →</a> · <a href="${sim.markdown}">仿真 Markdown 解释</a></p></section>`;
 const note=m.id==='00-conventions'?'':`<div class="notice">本组继承 <a href="00-conventions.html">共同模型与假设</a>。${m.discrepancy_ids.length?'含教材符号、近似或模型差异，请连同相应说明阅读。':'「精确」指指定小信号模型内精确。'}</div>`;
 const gallery=`<details class="card"><summary>来源幻灯片与讲解定位（${m.sources.length} 张）</summary><div class="source-grid">${m.sources.map(s=>`<figure><a href="${s.image}"><img loading="lazy" src="${s.image}" alt="原书幻灯片 ${s.slide_id}"></a><figcaption>${s.slide_id} · PDF ${s.pdf_page}／书本 ${s.printed_page} · <a href="../../extraction/index.html#SANSEN-${s.slide_id}">原图及完整讲解</a></figcaption></figure>`).join('')}</div></details>`;
 const body=`<div class="links"><a href="${m.id}.md">本组 Markdown</a><a href="full.md">全章 Markdown</a>${m.sfg?`<span class="tag">Physical / Causal SFG</span>`:''}${m.discrepancy_ids.length?'<a href="discrepancies.html" class="tag issue">含来源差异注记</a>':''}</div>${note}<article>${html}</article>${gallery}<div class="next">${i?`<a href="${mods[i-1].id}.html">← ${esc(mods[i-1].title)}</a>`:'<span></span>'}${i<mods.length-1?`<a href="${mods[i+1].id}.html">${esc(mods[i+1].title)} →</a>`:''}</div>`;
 fs.writeFileSync(`${out}/${m.id}.html`,page(m.title,m.id,body));
 data[m.id]={...m,html:html.replace(/(src|href)="(assets\/|physical_sfg\/|[.][.]\/)/g,(all,attr,rel)=>`${attr}="../derivations/ch02/${rel}`)};
}
const sym=verify.symbolic_checks.length,num=verify.numerical_nodal_checks;
const numberPoints=num.reduce((a,x)=>a+(x.samples||x.points||0),0);
const tiles=modules=>modules.map(m=>`<a class="tile" href="${m.id}.html"><span class="muted">${m.id.slice(0,2)} · 幻灯片 ${esc(m.slides.join('、'))}</span><h3>${esc(m.title)}</h3><p>${esc(m.summary_zh)}</p>${m.sfg?'<span class="tag">SFG</span> ':''}${m.discrepancy_ids.length?'<span class="tag issue">含差异注记</span>':''}</a>`).join('');
const caseSections=`<section aria-label="SFG 分析案例"><h2>需要 SFG 的三组案例</h2><p class="muted">已依据电路结构判断，保留必要的反馈与前馈路径。其他分析方法更直接的案例收在下方。</p><div class="grid">${tiles(sfgMods)}</div></section><details class="card" style="margin-top:24px" id="other-derivations"><summary>其他推导（${otherMods.length} 组）· 展开查看</summary><p class="muted">保留完整推导、近似条件和仿真入口，按需阅读。</p><div class="grid">${tiles(otherMods)}</div></details>`;
const coverageTable=`<details class="card"><summary>逐张幻灯片对照：72 / 72</summary><table><thead><tr><th>编号</th><th>PDF／书本页</th><th>推导位置</th><th>处理状态</th></tr></thead><tbody>${coverage.map(c=>`<tr><td><a href="../../extraction/index.html#SANSEN-${c.slide_id}">${c.slide_id}</a></td><td>${c.pdf_page} / ${c.printed_page}</td><td><a href="${c.module_id}.html">${esc(c.title)}</a></td><td>${c.status.startsWith('context')?'脉络页，无独立推导':c.discrepancy_ids.length?'推导＋来源差异':'已创建模型推导'}</td></tr>`).join('')}</tbody></table></details>`;
fs.writeFileSync(`${out}/index.html`,page('第二章 · 公式推导','index',`<section class="card"><h2>放大器、跟随器与共源共栅级</h2><p>默认展示需要 SFG 的三组案例，其余 17 组推导折叠保留。全章仍覆盖 72 张幻灯片，每组均保留电路方程、必要近似、适用条件和分析方法。</p><div class="stats"><div><strong>20</strong><span>推导组</span></div><div><strong>72 / 72</strong><span>幻灯片已逐张对照</span></div><div><strong>3</strong><span>必要的 SFG</span></div><div><strong>${sym}</strong><span>符号一致性检查通过</span></div></div><div class="notice">66 张技术内容纳入推导，另 6 张为章节脉络。${diffs.length} 项来源差异另列；其中 0244 的部分原模型／符号仍待厘清，并未宣称所有教材原式均已证明。</div><div class="links"><a href="00-conventions.html">开始阅读：共同假设 →</a><a href="full.md">完整 Markdown</a><a href="discrepancies.html">来源差异表</a></div></section>${caseSections}<section class="card" style="margin-top:24px"><h2>验算范围</h2><p>${sym} 项符号检查、${num.length} 组模型的随机复数频率节点方程对照均通过。验算的是明列模型与闭式公式的一致性；教材原式不一致之处另有条件说明，${hasSim?'实际 Spectre 结果见下方仿真入口。':'尚无 SPICE 或工艺模型验证。'}</p><p><a href="verification.json">完整数值与残差记录</a></p>${hasSim?'<div class="notice"><strong>已添加实际 Spectre 仿真</strong><p>20 组推导的代表模型，共 47 项检查通过。包含线性等效电路、gm/Id 法确定尺寸的 MOS1 电路、PZ 与瞬态。</p><a href="../../simulations/ch02/index.html">查看仿真报告与波形 →</a></div>':''}</section>${coverageTable}`));
fs.writeFileSync(`${out}/discrepancies.html`,page('教材差异与待厘清','differences',`<article>${md(fs.readFileSync(`${out}/discrepancies.md`,'utf8'))}</article>`));
fs.writeFileSync(`${out}/modules_data.json`,JSON.stringify(data));
fs.writeFileSync(`${out}/render_verification.json`,JSON.stringify({status:'passed',renderer:'marked + KaTeX (throwOnError=true)',math_expressions_rendered:mathCount,module_pages:mods.length,source_coverage:coverage.length,missing_math_placeholders:0,scope:'Static Markdown/TeX compilation; browser visual QA not performed.'},null,2));
console.log(JSON.stringify({status:'passed',module_pages:mods.length,math_expressions_rendered:mathCount}));
